# Competition edition: never commit a real credential.
# Configure the model from the local settings dialog after startup.
LLM_PROVIDER=mock
MEMORY_EMBEDDING=off

# Optional non-secret defaults shown by the local settings API.
# OPENAI_COMPAT_BASE_URL=https://example.invalid/v1
# OPENAI_COMPAT_MODEL=your-model-name

